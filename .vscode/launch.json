{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug pipeline-maxhit",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/install",
            "args": [
                "-pipeline", "path/to/pipeline",
                "-entry", "OpenGame",
                "-defaults", "path/to/default_pipeline.json"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}
